program TRIGONOMETRIC_PRG{
	version TRIGONOMETRIC_VER{
		float seno(float operando) = 1;
		float coseno(float operando) = 2;
		float tangente(float operando) = 3;
		float cotangente(float operando) = 4;
		float secante(float operando) = 5;
		float cosecante(float operando) = 6;
		float logaritmo(float operando) = 7;
		float raizCuadrada(float operando) = 8;
	} = 1;
} = 0x20000002;